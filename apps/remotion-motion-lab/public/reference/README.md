# 参考動画（CleanShot）

`CleanShotReferencePlayback` コンポジション用のファイルをここに置きます。

- **ファイル名（正）**: `cleanshot-reference.mp4`
- **移行**: 以前 `three-ranking-cleanshot-ref.mp4` だけがある場合は、同内容を `cleanshot-reference.mp4` として複製してください（コンポ ID・ThreeRanking とは無関係です）。
- **ソース例**: CleanShot 収録（パスは環境ごとに異なります）
- **注意**: `*.mp4` は `.gitignore` 対象です。別マシンでは手動コピーしてください。

# v1.0 Codex Handoff (2026-04-19 - part 6, post-cross-filter restore + migration completion audit prep)

This document is the authoritative cold-start handoff for the **next
chat whose job is to determine whether the WebGL -> WebGPU migration is
actually complete** on the current local feature branch.

This is **not** another "restore the missing visible effect" handoff.
The immediate visible blocker from the previous chats was addressed.
The next chat should start from an **audit / completion-judgment**
mindset, not from an assumption that migration is done or not done.

Read this file first.

Then read, in this order:

1. `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part5.md`
2. `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part4.md`
3. `packages/film-lab-renderer/src/webgpu/WebGPUBackend.ts`
4. `packages/film-lab-renderer/src/webgl/WebGLBackend.ts`

Part 4 remains the authoritative background for the already-landed
export / diffusion / preview parity bundle from `da74f188`.
Part 5 remains the authoritative record of the cross-filter visible
restore session and the Y-flip regression investigation.
This part-6 document supersedes them only for **current state at HEAD**
and for the **next-chat goal**.

## 1. Current repo state

- Repository root:
  `/Volumes/SamsungPortableSSDX5001/documents/forestone/chibatakumi-portfolio`
- Active branch:
  `feature/webgpu-migration-v1`
- Current local HEAD:
  `d04b3c3d8e823a4d4df2e5faab5b0f84b2050fcd`
- Branch relation at handoff time:
  local branch is ahead of `origin/feature/webgpu-migration-v1` by 3 commits
- Do not touch:
  `main`

Recent history, newest first:

- `d04b3c3d` `fix(ui): wrap cross-filter spikes hint in panel`
- `eb69b8b0` `fix(webgpu): restore cross-filter and glow orientation`
- `da74f188` `fix(webgpu): restore export and preview parity`
- `4466425c` `docs(webgpu-migration): 2026-04-19 part-3 handoff (post-QA, unresolved gaps)`
- `2e2ce7a1` `docs(webgpu-migration): 2026-04-19 part-2 handoff (post-parity-fix)`
- `66348c2e` `docs(webgpu-migration): ingest 2026-04-18 + 2026-04-19 codex handoffs`
- `ca5de309` `fix(webgpu): composite parity — glow orientation, grain, lens softness`
- `07a9ae2b` `feat(webgpu): explicit offscreen session + renderer-native readback`

### 1.1 Working tree state to preserve

At the time of writing this handoff, the local working tree still
contains unrelated dirty / untracked paths that must not be cleaned up
or staged accidentally:

- modified, unrelated:
  - `packages/film-lab-ui/node_modules/next-intl`
- untracked, intentionally present in this local workspace:
  - `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part4.md`
  - `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part5.md`
  - `.ai/knowledge/2026-04-19-film-lab-panel-flex-overflow.md`
  - `apps/web/output/`
  - `apps/web/src/features/motion/reference-works/futurism-poster-04-editorial-glass/`
  - `output/playwright/temporal-echo-benchmark-check/...`

The next chat should assume those are **not migration work** unless the
user explicitly changes scope.

## 2. User directives and constraints still in force

These were explicit in the prior migration chats and should be treated
as active unless the user changes them:

- Stay on `feature/webgpu-migration-v1`
- Do not touch `main`
- Do not introduce WebGPU -> WebGL backend fallback
- Ignore the unrelated dirty / untracked paths listed above
- Use `packages/film-lab-renderer/src/webgl/WebGLBackend.ts` as the
  runtime behavior source of truth for parity judgment
- Use `sequential-thinking` for load-bearing reasoning
- Optimize for accuracy over speed
- Minimize user decision cost
- Agent teams / parallel work are allowed
- If a fact is uncertain, verify it from code or docs rather than
  inheriting assumptions
- If local code and old handoff text disagree, trust **current code**

Additional instruction from the last user message before this handoff:

- the next chat is expected to **inspect / audit whether migration is
  complete**
- the output should make it possible to decide whether WebGPU migration
  is actually done or still incomplete

## 3. What was already landed before the latest local commits

This is the short version. Read part 4 for deep detail.

By commit `da74f188`, the following had already landed:

- WebCodecs + WebGPU export freeze fix
- backend-aware first-frame export fix
- WebGPU-first preflight cleanup
- `aberrationEdgeSoften` parity fix
- diffusion port / composite wiring
- preview/export parity bundle sufficient to move the visible blocker
  forward to cross-filter

That bundle is already documented in:

- `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part4.md`

The next chat should **not** reopen that work unless a completion audit
proves some part of it is still broken.

## 4. What landed after `da74f188`

### 4.1 Commit `eb69b8b0` - WebGPU cross-filter runtime restore + glow orientation fix

This was the migration-critical commit in this thread.

Changed file:

- `packages/film-lab-renderer/src/webgpu/WebGPUBackend.ts`

What this commit added or changed:

- WebGPU runtime wiring for cross-filter:
  - peak pipeline
  - peak-spacing-max pipeline
  - peak-spacing pipeline
  - streak pipeline
  - blend pipeline
- cross-filter bind-group layouts / uniform buffers / working targets
- black fallback texture for unused streak slots
- dedicated `renderCrossFilter(...)` helper
- insertion of cross-filter into the post tail before motion blur
- display vs offscreen flag split:
  - `displayFlagsBuffer` / `displayFlagsBindGroup`
  - `offscreenFlagsBuffer` / `offscreenFlagsBindGroup`
- dedicated cross-filter orientation flags:
  - `crossFilterFlagsBuffer` / `crossFilterFlagsBindGroup`
- the final glow-orientation fix:
  - generic `renderPyramidChain(...)` prefilter and downsample were
    restored to `offscreenFlagsBindGroup`
  - upsample stayed on `offscreenFlagsBindGroup`
  - final present/readback stayed on `displayFlagsBindGroup`

The cross-filter visible-restore work and the regression history before
that final orientation fix are documented in part 5.

### 4.2 Commit `d04b3c3d` - desktop/shared UI overflow follow-up

This is **not the migration core**, but it happened in the same thread
and should not be confused with renderer parity work.

Changed file:

- `packages/film-lab-ui/src/FilmLabControlPanelCore.tsx`

What changed:

- fixed unwanted horizontal scroll in the cross-filter panel row where
  the long Japanese `crossFilterSpikesHint` and the segmented control
  shared one flex row
- parent row gained `min-w-0`
- the left label/hint column became a fixed-width column
- the hint text gained proper breaking behavior
- the right segmented-control column became `min-w-0 flex-1`

This commit matters only because the next chat may inspect screenshots
or the current local UI and should know that the horizontal scroll issue
is a **separate UI follow-up**, not part of the WebGPU parity question.

## 5. Current user-visible state at handoff time

### 5.1 Migration-related visible state

Most recent user report after the renderer fixes:

- 「課題は解決できたようです」

Interpretation:

- the specific visible migration blocker from the previous chat
  (cross-filter missing / glow vertically inverted after the orientation
  work) appears resolved from the user's local manual check

Important caution:

- this is **not** the same as a formal proof that the entire WebGL ->
  WebGPU migration is complete
- it only means the user no longer sees the previously reported
  cross-filter / glow issue

### 5.2 UI follow-up state

The user then reported a **different** issue:

- unnecessary horizontal scroll caused by incorrect wrapping in the
  cross-filter UI

Relevant screenshots:

- `/Users/chibatakumi/Library/Application Support/CleanShot/media/media_XAChUSrzxd/CleanShot 2026-04-19 at 02.19.18@2x.png`
- `/Users/chibatakumi/Library/Application Support/CleanShot/media/media_TXLfqee3gM/CleanShot 2026-04-19 at 02.19.24@2x.png`

That was addressed in `d04b3c3d`.

## 6. Current code facts that the next chat must treat as ground truth

### 6.1 Current WebGPU render-path summary at HEAD

`packages/film-lab-renderer/src/webgpu/WebGPUBackend.ts` now describes
the active WebGPU path as:

1. filmlab -> `rt.colorGraded`
2. bloom pyramid
3. halation pyramid
4. diffusion pyramid
5. composite -> `rt.composited`
6. post chain:
   - cross-filter (if enabled)
   - motion blur (if enabled)
   - final blit / readback

This summary is visible near the file header.

### 6.2 Generic bloom / halation pyramid orientation is back on the neutral offscreen contract

In current HEAD:

- `renderPyramidChain(...)` prefilter uses `offscreenFlagsBindGroup`
- `renderPyramidChain(...)` downsample uses `offscreenFlagsBindGroup`
- `renderPyramidChain(...)` upsample uses `offscreenFlagsBindGroup`

Current line references at HEAD:

- prefilter: around `WebGPUBackend.ts:1377`
- downsample: around `WebGPUBackend.ts:1420`
- upsample: around `WebGPUBackend.ts:1464`

This is important because the accidental leak of
`crossFilterFlagsBindGroup` into the generic pyramid helper was the
strongest root-cause candidate for the upside-down glow / light-bleed
regression, and that leak is no longer present.

### 6.3 Display/readback flip split is still intentionally present

Current WebGPU behavior:

- `setFlipY(...)` writes only `displayFlagsBuffer`
- final present / readback passes use `displayFlagsBindGroup`
- intermediate offscreen passes use `offscreenFlagsBindGroup`

Current line references at HEAD:

- `setFlipY(...)`: around `WebGPUBackend.ts:1078`
- blit readback/present: around `WebGPUBackend.ts:2231`, `2248`
- motion blur readback/present: around `WebGPUBackend.ts:2367`, `2385`

### 6.4 The cross-filter chain itself is still internally mixed

At current HEAD:

- threshold uses `offscreenFlagsBindGroup`
- peak uses `offscreenFlagsBindGroup`
- spacing-max-x uses `crossFilterFlagsBindGroup`
- spacing-max-y uses `crossFilterFlagsBindGroup`
- spacing uses `crossFilterFlagsBindGroup`
- streak uses `crossFilterFlagsBindGroup`
- blend uses `crossFilterFlagsBindGroup`

Current line references at HEAD:

- threshold: around `WebGPUBackend.ts:1715`
- peak: around `WebGPUBackend.ts:1754`
- spacing-max-x: around `WebGPUBackend.ts:1808`
- spacing-max-y: around `WebGPUBackend.ts:1852`
- spacing: around `WebGPUBackend.ts:1892`
- streak: around `WebGPUBackend.ts:1953`
- blend: around `WebGPUBackend.ts:1998`

This mixed state is currently tolerated only because the user-reported
visible blocker appears solved. It still matters for the next chat's
completion audit.

### 6.5 WebGL source of truth still has a larger post chain than WebGPU

In `packages/film-lab-renderer/src/webgl/WebGLBackend.ts`,
`renderPostCompositeChain(...)` still runs:

- `CrossFilter -> Shafts -> Dust -> MotionBlur`

Current line references:

- `renderPostCompositeChain(...)`: around `WebGLBackend.ts:1476`
- pass order comment: around `WebGLBackend.ts:1470`

By contrast, current `WebGPUBackend.ts` only routes the post-composite
output through:

- cross-filter
- motion blur
- final blit/readback

Important supporting facts in current WebGPU code:

- `lightshafts` / `lightshaftsBlend` / `dust` WGSL modules are imported
  and created as shader modules
- but there is no active WebGPU render integration for those passes in
  the current backend
- there are also no WebGPU setter / param-consumption paths equivalent
  to the WebGL `setDustAmount`, `setShaftIntensity`, `setShaftDecay`,
  `setShaftOriginX`, `setShaftOriginY` methods

This is a major audit target. Do not assume migration is "complete"
without deciding how these missing WebGL post passes should be treated.

### 6.6 Split / A-B compare is still not present in the active WebGPU backend

Current WebGPU facts:

- file header still says `split/A-B compare` is pending
- `frameState.splitPosition` exists
- `setSplitPosition(...)` / `getSplitPosition()` exist
- but there is no compare-pair state machine or compare render path like
  WebGL's `abCompareEnabled`, `setComparePair(...)`, or compare render
  flow

Current WebGPU references:

- pending comment: around `WebGPUBackend.ts:43-45`
- `splitPosition` state init: around `WebGPUBackend.ts:322`
- `setSplitPosition(...)`: around `WebGPUBackend.ts:1013`

Current WebGL references:

- `abCompareEnabled` state: around `WebGLBackend.ts:186`
- `setComparePair(...)`: around `WebGLBackend.ts:1526`
- compare render flow: around `WebGLBackend.ts:1577+`

This is another major audit target.

### 6.7 The current WebGPU file itself still declares remaining pending work

The current header in `WebGPUBackend.ts` still says:

- `Cross-filter Hard Mode temporal / central bloom parity`
- `split/A-B compare`
- `dust`

Important:

- the file header is not automatically authoritative by itself
- but it is a strong signal that the migration was not yet declared
  complete inside the implementation
- the next chat must reconcile this file-header statement with the
  current shipped user-visible state and with the user's definition of
  "complete"

## 7. Very important nuance: do not confuse starter states with mode parity

The current shared UI distinguishes:

### 7.1 Cross starter states ("Light" / "Strong")

These are the two convenience starter buttons in the cross-filter card.
In code they live in:

- `packages/film-lab-ui/src/FilmLabControlPanelCore.tsx`

`CROSS_STARTER_STATES` currently force:

- `crossFilterHardMode: 1`

for both starter presets.

### 7.2 Soft / Hard mode semantics

The UI copy currently says:

- `Soft mode is currently frozen. Cross Filter runs in Hard mode only for now, while the Soft path stays in the codebase for future development.`

Relevant messages:

- `apps/desktop-film-lab-batch/messages/en.json`
- `apps/desktop-film-lab-batch/messages/ja.json`

This means the next chat must not make a shallow assumption such as:

- "the user accepted Strong-only starter state, therefore Hard Mode
  parity no longer matters"

Those are different concepts.

The next chat should explicitly decide whether:

- the remaining `Hard Mode temporal / central bloom parity` note is a
  real migration blocker for the currently shipped product surface
- or whether the current UI / shipping intent intentionally narrows the
  acceptance bar enough that those paths are allowed to remain deferred

Do not guess. Verify from code + product surface + prior docs.

## 8. Verification already run in this thread

### 8.1 After `eb69b8b0`

The following commands were run and passed:

- `cd /Volumes/SamsungPortableSSDX5001/documents/forestone/chibatakumi-portfolio/packages/film-lab-renderer && bunx tsc --noEmit`
- `cd /Volumes/SamsungPortableSSDX5001/documents/forestone/chibatakumi-portfolio/packages/film-lab-renderer && bun run build`
- `cd /Volumes/SamsungPortableSSDX5001/documents/forestone/chibatakumi-portfolio/apps/desktop-film-lab-batch && bun run test`

Recorded results:

- renderer typecheck passed
- renderer build passed
- desktop app tests passed
- desktop test summary included `16 passed` files and `60 passed` tests

### 8.2 After `d04b3c3d`

The following command was run and passed:

- `cd /Volumes/SamsungPortableSSDX5001/documents/forestone/chibatakumi-portfolio/apps/desktop-film-lab-batch && bun run test`

Recorded result:

- `16 passed` files / `60 passed` tests

### 8.3 What these checks do **not** prove

These checks do **not** prove:

- that WebGL -> WebGPU migration is fully complete
- that all WebGL post effects have corresponding WebGPU render-path
  integration
- that compare / split parity exists
- that light shafts or dust parity exists
- that hard-mode cross-filter parity is fully achieved

They only prove that the current code typechecks, builds, and passes the
existing app-side automated tests.

## 9. What the next chat must answer

The next chat's primary question is:

> Is the WebGL -> WebGPU migration complete on the current
> `feature/webgpu-migration-v1` branch, and if not, exactly what remains?

That should be answered with a code-verified audit, not intuition.

The next chat should explicitly decide:

1. Which features / paths are considered in-scope for "migration complete"
2. Which of those are already implemented on WebGPU
3. Which are intentionally frozen / deferred
4. Which are still genuinely missing relative to WebGL
5. Whether the current state should be called:
   - complete
   - complete-for-v1 with documented defers
   - not complete

## 10. Recommended audit procedure for the next chat

### 10.1 Read order

1. This part-6 handoff
2. Part 5
3. Part 4
4. Current `WebGPUBackend.ts`
5. Current `WebGLBackend.ts`

### 10.2 Build a parity matrix

At minimum, audit these categories:

- primary grade / LUT path
- bloom
- halation
- diffusion
- composite parity
- cross-filter visible path
- cross-filter hard-mode temporal / central bloom parity
- light shafts
- dust / scratches
- motion blur
- display/readback orientation
- export / offscreen session path
- split / A-B compare
- public param coverage vs actual render-path consumption

For each category, mark:

- implemented
- partially implemented
- intentionally deferred
- missing

and cite the code that proves the judgment.

### 10.3 Do not trust old text over current code

If old handoff docs say a thing is deferred, but current code proves it
is live, trust code.

If current code still has no path for a feature that WebGL actively
renders, do not call it complete just because a visible blocker was
recently fixed.

### 10.4 Reconcile product-surface reality with code comments

This is the subtle part.

The next chat should reconcile all three:

- current code
- current UI surface
- historical handoff / defer text

Example:

- UI currently says cross-filter runs in Hard mode only
- WebGPU file header still says Hard Mode temporal / central bloom
  parity is pending

That tension must be resolved explicitly in the audit result.

### 10.5 Avoid expanding into implementation before the audit is clear

The next chat should begin in audit mode.

Only after the audit is clear should it move into implementation, and
only if the user asks for that next step.

## 11. Explicit non-goals for the next chat unless the user changes scope

- do not touch `main`
- do not add WebGPU -> WebGL fallback
- do not clean unrelated dirty / untracked paths
- do not reopen the already-landed export / diffusion / aberration fixes
  without code evidence
- do not assume "issue solved" means "migration complete"
- do not start coding missing parity work before first answering the
  completion audit question

## 12. Files the next chat will likely need immediately

Migration docs:

- `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part6.md`
- `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part5.md`
- `apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part4.md`

Renderer source of truth:

- `packages/film-lab-renderer/src/webgpu/WebGPUBackend.ts`
- `packages/film-lab-renderer/src/webgl/WebGLBackend.ts`
- `packages/film-lab-renderer/src/webgpu/compositeUniforms.ts`
- `packages/film-lab-renderer/src/webgpu/MediaTexture.ts`

Offscreen/export path:

- `apps/desktop-film-lab-batch/src/renderer/offscreen/webgpu-offscreen-render-session.ts`
- `apps/desktop-film-lab-batch/src/renderer/video-export-pipeline.ts`
- `apps/desktop-film-lab-batch/electron/main.ts`

Secondary UI context:

- `packages/film-lab-ui/src/FilmLabControlPanelCore.tsx`
- `apps/desktop-film-lab-batch/messages/ja.json`
- `apps/desktop-film-lab-batch/messages/en.json`

## 13. Cold-start summary

The last visible migration blocker in this thread was the WebGPU
cross-filter path.

That blocker was addressed by:

- adding the missing WebGPU cross-filter runtime wiring
- fixing the accidental generic-pyramid orientation contamination that
  made glow / light bleed appear vertically inverted

The user then reported that the issue now appears solved.

However, current HEAD still shows strong evidence that the migration may
not yet be globally complete:

- `WebGPUBackend.ts` still self-documents pending work
- WebGPU post chain is still smaller than WebGL's post chain
- split / A-B compare is not present in active WebGPU code
- dust and light shafts are not in the active WebGPU render path
- cross-filter internal orientation contract is still mixed

So the next chat should not start from either of these assumptions:

- "migration is definitely complete"
- "migration is definitely incomplete"

It should perform a feature-by-feature parity audit and then answer that
question precisely.

## 14. High-Precision English Handoff Prompt

```text
You are continuing the Filmtone WebGL -> WebGPU migration audit in the existing workspace.

Your job in this chat is NOT to blindly continue implementation. Your first job is to determine, with code-backed precision, whether the migration is actually complete on the current feature branch.

Repository and branch:
- Repo root: /Volumes/SamsungPortableSSDX5001/documents/forestone/chibatakumi-portfolio
- Branch: feature/webgpu-migration-v1
- Current local HEAD: d04b3c3d8e823a4d4df2e5faab5b0f84b2050fcd
- Do not touch main

Read in this exact order:
1. apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part6.md
2. apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part5.md
3. apps/desktop-film-lab-batch/docs/webgpu-migration/v1.0-codex-handoff-2026-04-19-part4.md
4. packages/film-lab-renderer/src/webgpu/WebGPUBackend.ts
5. packages/film-lab-renderer/src/webgl/WebGLBackend.ts

Critical constraints:
- Use sequential-thinking for any load-bearing reasoning.
- Accuracy over speed.
- Do not introduce WebGPU -> WebGL backend fallback.
- Do not touch main.
- Ignore unrelated dirty/untracked paths already documented in the handoff.
- If old docs and current code disagree, trust current code.
- Minimize user decision cost.
- Agent teams / parallel work are allowed if useful.

Most important context:
- The previous migration blocker was WebGPU cross-filter runtime wiring.
- That visible issue was addressed in commit eb69b8b0.
- The user then reported that the problem now appears solved.
- However, current HEAD still contains strong evidence that migration may not be globally complete:
  - WebGPUBackend.ts still says pending: cross-filter Hard Mode temporal / central bloom parity, split/A-B compare, dust.
  - WebGL renderPostCompositeChain() still includes CrossFilter -> Shafts -> Dust -> MotionBlur.
  - Current WebGPU active post path appears to include cross-filter and motion blur only.
  - WebGPU has splitPosition state but no active compare-pair render path comparable to WebGL.
  - Current WebGPU cross-filter orientation contract is still internally mixed.

Your required deliverable:
1. Audit whether the migration is complete.
2. Build a precise parity matrix between WebGL and WebGPU for at least:
   - primary grade / LUT path
   - bloom
   - halation
   - diffusion
   - composite
   - cross-filter visible path
   - cross-filter hard-mode temporal / central bloom parity
   - light shafts
   - dust / scratches
   - motion blur
   - display / readback orientation
   - export / offscreen session path
   - split / A-B compare
   - param surface vs actual render-path consumption
3. For each category, classify it as:
   - implemented
   - partially implemented
   - intentionally deferred
   - missing
4. Explicitly answer one of:
   - migration complete
   - migration complete for v1 with explicit defers
   - migration not complete
5. Cite the code that proves your judgment.

Important nuance:
- Do not confuse the Cross Filter “Light / Strong” starter states with Soft / Hard mode parity.
- The UI currently says Soft mode is frozen and Cross Filter runs in Hard mode only for now.
- That means you must explicitly decide whether the remaining “Hard Mode temporal / central bloom parity” note is still a real blocker for completion.

Default approach:
- Start in audit mode, not implementation mode.
- Only propose or perform code changes after the audit result is clear and only if needed.
- If you need to inspect behavior outside local code, search or browse, but local source should be your primary evidence.

At the end, give:
- a concise verdict
- the parity matrix
- the exact remaining blockers, if any
- the minimum next action to reach true completion, if completion has not yet been achieved
```

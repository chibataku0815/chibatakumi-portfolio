// @chibatakumi/motion-grid — Renewal 2026 Phase 1 / Stream 1 (scaffold).
// Source: life/output/motion-grid-guided-webgpu
// Wraps as MotionParticipant. Used at /experiments/grid (full intensity + text input)
// and as nav grid vocabulary in /works.

export { createGridParticipant, GRID_WIRING } from "./participant";
export type { GridParams, CreateGridParticipantOptions } from "./participant";

export const GRID_PACKAGE_VERSION = "0.1.0-phase-b";

# Stream 5 Status — Filmtone Subsumption + Audio Integration

| 項目 | 値 |
|---|---|
| 親計画 | `life/.claude/plans/portfolio-renewal-2026-04.md` §7.5 |
| 最終更新 | 2026-04-26 (Wave 2 完了時) |
| 完了状態 | 🟢 D5.1-D5.6 全 landed / D5.7 partial (Wave 2 分 inline canonical 完了、motion-studies + doc は Wave 3) |

## Deliverables

- [x] **D5.1** `/works/filmtone/*` 内容再構成 → **Wave 2 で landed** (Agent α)
  - 旧 `/[locale]/film-lab/*` 12 file 全削除、新 `/[locale]/works/filmtone/*` 14 file 新規作成
  - 8 sub-route: top + privacy + signature + roadmap + release-notes + support (+ thanks nested) + download (+ complete nested) + og
  - layout.tsx に `<div data-theme="dark" className="film-lab-lp-root">` wrapper 付与 (D3.3 finalize 兼)
  - top page (`/works/filmtone/page.tsx`) は server component で static + 1 client island (`<FilmLabFullPage />`) 構造を継承 (D5.6 兼)
  - SEO canonical inline 8 page (D5.7 Wave 2 分兼)
  - features dir の internal href 5 file も補完更新 (FilmLabFullPage / DownloadComplete / ReleaseNotesContent / RoadmapContent / Showcase)
  - Stripe / waitlist / smart-look 動的依存を保持
  - **Wave 3 punt 候補 (cosmetic)**: namespace `useTranslations("film-lab.*")` → `useTranslations("works.filmtone.*")` 24 箇所 (URL migration の本質達成済、namespace は cosmetic、user 任意)

- [x] **D5.2** API ルート整理 → **Wave 2 で landed** (Agent α)
  - `/apps/web/src/app/api/waitlist/route.ts` 新規作成 (本体ロジック移管)
  - 旧 `/api/film-lab/waitlist/route.ts` を re-export shim に置換 (`export { POST } from "../../waitlist/route"; export const runtime = "nodejs";` Next 16 + Turbopack の `runtime` field re-export 不可制約に対応)
  - internal caller (`filmtone-signature-waitlist-client.tsx:18`) の fetch path を `/api/waitlist` に更新
  - `/api/film-lab/donation/*` `/api/film-lab/ai/*` そのまま維持 (Filmtone 独立ドメイン化までの暫定)
  - **Smoke verified**: `POST /api/waitlist` と `POST /api/film-lab/waitlist` (alias) 両方 400 (validation rejection) で同 behavior、handler 到達確認

- [x] **D5.3** `GlobalAudioController` 実装 (Wave 1 Agent E)
  - landed: `apps/web/src/features/audio/` boundary、4 file
    - `GlobalAudioController.ts` — singleton wrapper、`createAudioController` (webgpu-motion-audio) を内包
    - `AudioBusProvider.tsx` — `'use client'` provider、`useAudioBus()` hook export
    - `types.ts` — public types re-export
    - `index.ts` — feature boundary export
  - storageKeyPrefix: `portfolio-audio` (hardcoded)
  - source: silent | file | mic 3 mode (controller 経由)
  - mute: wrapper flag + localStorage persist (実 silencing wiring は Wave 2 D5.4 で SoundToggle に接続)
  - notes: `AudioBus.ensureAudioContext()` は private のため `controller.toggle()` 経由で context resume (gesture-required)
  - workspace dep: `apps/web/package.json` に `webgpu-motion-audio: workspace:*` 追加 (Wave 1 で Agent E 実施)

- [x] **D5.4** Sound icon UI 全ページ右下 fixed → **Wave 2 で landed** (Agent β)
  - 新規作成: `apps/web/src/features/audio/SoundToggleControl.tsx` (`useAudioBus()` 経由、SoundState `silent | playing | muted` 派生、fixed bottom-right z-50 inline style)
  - root layout (`[locale]/layout.tsx`) に `<AudioBusProvider>` を `<MotionStageProvider>` 内側に配置 + `<SoundToggleControl />` を mount
  - gesture-gate flow: click → `audio.ensureContext()` → `controller.toggle()` → AudioContext.resume + element.play → `audio.setMute()` → localStorage persist
  - `feedback_no_fallback_bug_hotbed.md` 厳守: AudioContext.resume reject 時は明示的 setMute(true) で silent fallback、retry なし

- [x] **D5.5** `/experiments/*` で mic input opt-in → **Wave 2 で landed** (Agent β)
  - 新規作成: `apps/web/src/features/audio/MicInputGate.tsx` (5 mode: preMount / unsupported / requesting / connected / blocked / idle)
  - `experiments/layout.tsx` に top-left fixed 配置 (右下 SoundToggle と非衝突)
  - Permission flow: `audio.setSource("input")` → vendor controller が `getUserMedia({audio:true})` invoke → grant `connected` / decline `blocked` / enumerate fail `error`
  - **Permission decline で自動 fallback しない**: 明示的「Mic blocked」UI 表示 (`feedback_no_fallback_bug_hotbed.md` 適用)

- [x] **D5.6** Filmtone case study ページの dynamic data 非依存化 → **Wave 2 で landed** (Agent α)
  - 既存の server component + 1 client island (`<FilmLabFullPage />`) 構造を継承
  - 「独立ドメイン化時の static snapshot 化容易性」目標は現行構造で達成済と判断
  - 将来 multi-island 化したい場合の hook を docblock に記録 (waitlist island / donation island の分離は behavior 変更を伴うため Wave 2 では実施せず)

- [~] **D5.7** Filmtone redirect SEO 維持 → **partial、3 段 finalize** (Wave 1 → Wave 2 → Wave 3)
  - **Wave 1 で landed**: 新 IA base route **6 件**に `metadata.alternates.canonical` を inline 設定 (works/photography, works/commercial, works/installation, craft, about, journal、portfolioData.site.siteUrl 経由)
  - **Wave 2 で landed**: Filmtone **8 page** (top + 6 sub + 2 nested) に canonical metadata inline 設定 (Agent α、ja/en alternates languages 整備)
  - **Wave 3 finalize 残**:
    - journal/motion-studies/{6 slugs} に canonical metadata 追加 (外殻 / SEO QA layer、6 file × 数行で軽微)
    - `docs/renewal-2026/seo-migration-procedure.md` doc finalize (Wave 1 skeleton 創建済)
    - Search Console 退役申請手順は production launch 後 ops

## Definition of Done (v3)

D5.1 / D5.2 / D5.3 / D5.4 / D5.5 / D5.6 全 closed (Wave 1 + Wave 2、達成済) + D5.7 Wave 3 finalize 残 (motion-studies 6 canonical + doc + ops)。audio toggle 全 route 動作 + mic input `/experiments/*` opt-in 動作 + Filmtone 全 page 新 path 経由動作 (Wave 2 達成)。

## Notes

- 2026-04-25 scope drift により Stream 5 自体が無自覚後送りされていた（plan §15 Postmortem）→ Wave 1 で D5.3 kickoff、Wave 2 で D5.1/D5.2/D5.4/D5.5/D5.6 並列実装で完全 closed
- D5.3 GlobalAudioController と Stream 4 D4.4 の MotionStageProvider が AudioBus 共有設計（plan §5.5）→ Wave 2 D5.4 で `<AudioBusProvider>` を `<MotionStageProvider>` 内側に配置することで wire-up 完了
- Filmtone 動的依存 (Stripe webhook / waitlist DB / smart-look) は API alias shim で silent breakage 回避済

# Stream 4 Status — Portfolio Shell Renewal

| 項目 | 値 |
|---|---|
| 親計画 | `life/.claude/plans/portfolio-renewal-2026-04.md` §7.4 |
| 最終更新 | 2026-04-26 (Wave 2 完了時) |
| 完了状態 | 🟢 D4.1-D4.13 landed (13/15) / D4.14/D4.15 Wave 3 QA Wave forward |

## Deliverables

### Landed (Wave 1 含む)

- [x] **D4.1** MotionStage core 実装
  - landed: `packages/motion-core/src/stage/{index.ts, composite.wgsl.ts}`

- [x] **D4.2** `apps/web/next.config.ts` `transpilePackages` 拡張
  - landed: 17 packages、本 Wave 1 で `redirects()` 追加 (D4.11 partial)

- [x] **D4.3** `apps/web/package.json` workspace deps 4 件追加
  - landed: motion-{core,dot,grid,flow}。Wave 1 で `@chibatakumi/design-system` (Agent C) と `webgpu-motion-audio` (Agent E) も追加、計 6 workspace deps

- [x] **D4.4** `apps/web/src/features/motion/` boundary
  - landed: MotionStageContext.ts / MotionStageProvider.tsx / MotionUnsupportedBanner / useExperimentParticipant.ts / index.ts

- [x] **D4.5** `/[locale]/experiments/{dot,grid,flow}` 3 route + experiments/layout.tsx
  - landed
  - **Wave 1 update**: experiments/layout.tsx の MotionStageProvider 撤去、root layout 移管 (Agent A)

- [x] **D4.6** motion-{dot,grid,flow} Phase A wiring
  - landed: createDotParticipant / createGridParticipant / createFlowParticipant の lifecycle 実装

### Wave 1 Landed (本 session)

- [x] **D4.7** **persistent `<MotionCanvas>` を root layout に移動** (Wave 1 Agent A)
  - landed: working tree
    - `apps/web/src/app/[locale]/layout.tsx` に MotionStageProvider 配置 (`<body>` 直下、NextIntlClientProvider の外側)
    - `experiments/layout.tsx` から MotionStageProvider 撤去、`<div>` + MotionUnsupportedBanner のみ残す
  - verification: 全 route で同一 canvas instance 維持を Wave 2 smoke check で確認予定 (D2.7 smoke)
  - blocker for D5.4 (Sound icon root 配置) を解消

- [x] **D4.8** **Home Hero 全面書き換え** (Wave 1 Agent A + post-merge carry-over fix)
  - landed: working tree
    - `apps/web/src/app/[locale]/page.tsx` 書換、`<HomeHero />` import 撤去
    - `apps/web/src/features/hero/components/AmbientHomeHero.tsx` 新規、`useExperimentParticipant` で dot scene 起動
    - 旧 HomeHero 系 9 file は orphan 化 (削除せず import のみ撤去、revert 容易性保持)
  - **採用 scene (post-merge fix 2026-04-26)**: `initialScene: "orbit"` + `enableSceneCycle: true` + `cycleScenes: ["orbit", "river", "firefly", "molecular"]`
    - 前 chat commit `e1e52b7a` (Phase A+1 polish: KineticHandoff scene cycle) が Wave 1 初期実装で carry されていなかった問題を post-merge で修正
    - 修正前: `initialScene: "river"` 単発、cycling 無効 (Agent A の意図的 calm 設計、ただし前 chat polish が ambient 背景に carry されていなかった + plan §4.4 "Hero = Scene 1 (orbit) idle 強度: 高" ともズレ)
    - 修正後: 4 scene curated subset で auto-cycle (~33s loop、計算: 5.5s play × 4 + 1.75s blend × 4 + 1.25s settle × 4)
    - showcase の 16 scene 全 cycle は `/experiments/dot` で継続、本 home は curated subset で calm 維持

- [x] **D4.9** **新 IA routes 実装** (Wave 1 Agent B)
  - landed: 13 新 route file
    - `[locale]/works/{photography,commercial,installation}/page.tsx` (3)
    - `[locale]/{craft,about,journal}/page.tsx` (3)
    - `[locale]/journal/motion-studies/{6 slugs}/page.tsx` (6 静的 routes、searchParams capture/frame/play forward)
    - 6 slug: anchored-progress-resolve, boiling-poster-aperture, motif-loop-background, signal-stroke-relay, staged-emphasis-payoff, temporal-echo-residue
  - notes: about/journal は minimal placeholder + i18n keys (full content port は Wave 2)

- [x] **D4.10** **旧 routes 解体** (Wave 1 Agent B)
  - landed: 7 dir 削除
    - `[locale]/{interactive,skills,profile,archive,motion,photography,installation}/`
    - photography は layout.tsx も drop (works/ flat group)
    - motion/reference-works/* は new journal/motion-studies/* に内容移植済 (Agent B)
  - **NOT touched**: `[locale]/film-lab/**` (Wave 2 F scope)

- [x] **D4.11** **Redirect map** → **Wave 2 で finalize landed** (Agent α、wildcard pattern 採用)
  - Wave 1 で landed: `next.config.ts redirects()` async function、**16 entries** (locale 2 形式 × 8 logical paths: photography / interactive / installation / motion/reference-works/:slug* / motion / skills / profile / archive)
  - **Wave 2 finalize**: Filmtone wildcard 4 entries 追加
    ```ts
    { source: "/film-lab", destination: "/works/filmtone", permanent: true },
    { source: "/film-lab/:path*", destination: "/works/filmtone/:path*", permanent: true },
    { source: "/:locale(en|ja)/film-lab", destination: "/:locale/works/filmtone", permanent: true },
    { source: "/:locale(en|ja)/film-lab/:path*", destination: "/:locale/works/filmtone/:path*", permanent: true },
    ```
  - 最終 entry 数: 16 (Wave 1 base) + 4 (Filmtone wildcard) = **20 entries**
  - **技術的最適化**: Director directive (Wave 1 後の post-audit) は「Filmtone 8 paths × 2 forms = 16 entries 追加」を explicit listing で指示したが、実装は **wildcard pattern (`/film-lab/:path*`) で 4 entries に集約**。理由: nested path (`/film-lab/support/thanks`, `/film-lab/download/complete`) を redirect 漏れなくカバーする合理的最適化。結果 16 (Wave 1 base) + 4 (Filmtone wildcard) = 20 entries (Director directive の数値「32」は explicit listing 前提、wildcard 採用で減少、誤計算ではなく実装方法の差)
  - **Smoke verified (Wave 2 main chat)**: 8 case 全 308 redirect 通過 (`/film-lab` `/film-lab/privacy` `/film-lab/signature` `/film-lab/support/thanks` `/film-lab/download/complete` `/ja/film-lab` `/en/film-lab` `/ja/film-lab/signature`)

- [x] **D4.12** Page transition orchestrator (Wave 1 Agent A)
  - landed: working tree
    - `apps/web/src/shared/transitions/PageTransition.tsx` を 260行 → 65行 に縮小
    - `useMotionStage` + `usePathname` 駆動の薄い orchestrator に置換
    - `stage.setActive(routeKeyToParticipantName(pathname), 500)` を path 変化で呼ぶ
    - 既存 GSAP block-reveal 撤去
    - missing-participant エラーは no-op で swallow

### Forward (Wave 2 / Wave 3)

- [x] **D4.13** next-intl 既存継承 → **Wave 2 で smoke 通過** (main chat δ verification)
  - Smoke verified: en routes 200 (`/en/works`, `/en/works/filmtone`, `/en/about`, `/en/craft`, `/en/journal` 等)、ja routes は next-intl `localePrefix: "as-needed"` で 307 → unprefixed redirect (default locale = ja のため `/ja/about` → `/about`、これは next-intl の正常動作)
  - i18n 切替動作確認、missing key なし
  - Wave 3 e2e で formal 検証 (D4.14)

- [ ] **D4.14** e2e tests 4 種 → **Wave 3 forward** (Director Edit 2)
  - core 全 visual approve 後の QA、Wave 2 中の途中検証は smoke (D2.7) のみ
  - 含む: 旧 18 path → 新 path redirect / i18n ja/en / WebGPU unsupported screen / persistent canvas re-mount 検証

- [ ] **D4.15** preview deploy 検証 → **Wave 3 forward** (Director Edit 2)
  - Vercel preview で submodule 解決 + WebGPU 動作 = final visual gate
  - D1.6 typing fix がここで表面化したら Wave 3 内で前倒し可

## Definition of Done (v3)

D4.1 〜 D4.13 全 closed (Wave 1 + Wave 2) + D4.14 e2e green (Wave 3) + D4.15 preview deploy 通過 (Wave 3)。

## Notes

- Wave 1 で D4.7-D4.12 を一気に landed (scope drift 復元)、Wave 2 で D4.11 wildcard finalize + D4.13 i18n smoke 通過
- D4.14 / D4.15 は plan §0.3 上の de-scope ではなく **phasing** (QA Wave)、Wave 3 で完了予定
- Wave 2 完了時 build verify: `bun run --cwd apps/web build` 成功、67 pages prerendered (13 workers)
- Wave 2 完了時 typecheck: **4 baseline errors 維持** (`layout.tsx:17` globals.css side-effect TS2882 + `desktop-release-info.test.ts:1` & `params-codec.test.ts:8` bun:test TS2307 × 2 + `params-codec.test.ts:87` Params cast TS2352、Wave 1 起因外、Director post-Wave 2 disk-verify audit で訂正)
- redirects() 最終 entries: **20 entries** = 16 (Wave 1 base) + 4 (Filmtone wildcard)。Director directive (Wave 1 後の post-audit) の explicit listing 想定値「32 entries」は実装が wildcard pattern 採用で 4 entries に集約され減少 (誤計算ではなく合理的最適化)

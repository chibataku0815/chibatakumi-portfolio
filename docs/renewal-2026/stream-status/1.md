# Stream 1 Status — Motion Core Library

| 項目 | 値 |
|---|---|
| 親計画 | `life/.claude/plans/portfolio-renewal-2026-04.md` §7.1 |
| 最終更新 | 2026-04-26 (Wave 1 完了時 audit) |
| 完了状態 | 🟡 D1.1-D1.5 landed / D1.6 Wave 3 forward (緊急度低) |

## Deliverables

- [x] **D1.1** `packages/motion-core` 新設
  - landed: commit `7e16c224` (feat(renewal-2026): Phase 1 motion-core + vendored webgpu-motion-libs submodule)
  - verification: bun install green、typecheck 0 errors

- [x] **D1.2** `webgpu-motion-{shell,audio,post,art,dom,input,scene,ui}` 8 packages 取り込み
  - landed: commit `7e16c224` 内、`vendor/webgpu-motion-libs/packages/*`
  - source: subtree split from life/packages/, mirrored at `https://github.com/chibataku0815/webgpu-motion-libs` (private)、HEAD `b96998b`

- [x] **D1.3** `gpu-{fx-presets, 2.5d-presets, film-post}` 3 packages 取り込み
  - landed: 同上 submodule

- [x] **D1.4** `MotionParticipant<T>` API + 型定義
  - landed: `packages/motion-core/src/participant/index.ts`
  - 含む: MotionStage / SceneSnapshot / AudioState

- [x] **D1.5** `packages/motion-grid` `packages/motion-flow` skeleton を participant 化
  - landed: scaffold で `createGridParticipant` / `createFlowParticipant` export
  - audio wiring 係数: GRID 3 wires、FLOW 8 wires (Phase 10 post-tune 反映)

- [ ] **D1.6** webgpu-motion-libs 側 typing fix → **Wave 3 forward** (Director Edit 2)
  - 状態: submodule HEAD `b96998b` で **実質完了確認済 (Wave 1 audit)**
  - audit 結果 (Wave 1 Agent E):
    - 11 packages のうち 7 packages (gpu-2.5d-presets / gpu-film-post / gpu-fx-presets / webgpu-motion-art / webgpu-motion-post / webgpu-motion-scene / webgpu-motion-shell) は env.d.ts + `@webgpu/types` 完備
    - 残 4 packages (webgpu-motion-audio / webgpu-motion-dom / webgpu-motion-input / webgpu-motion-ui) は env.d.ts なしだが、いずれも `*.wgsl?raw` import なし & `GPU*` 型参照なしのため env.d.ts 不要
    - motion-{core,dot,grid,flow} 全 4 packages typecheck **0 errors**
  - Wave 3 で D4.15 Vercel preview deploy が表面化したら前倒し可、それ以外は緊急度低
  - notes: Wave 1 audit で実質 closed と判定したが、formal `[x]` は Wave 3 で D4.15 verify 通過後に確定

## Definition of Done

D1.1 〜 D1.5 全 closed (達成済) + D1.6 は Wave 3 で formal close + portfolio `bun install` green + typecheck 0 errors (達成済)。

## Notes

- D1.6 が closed しなくても motion-* package の使用には支障なし (Wave 1 で apps/web build green を確認済)
- Wave 3 で D4.15 preview deploy 時に submodule resolution が問題になったら D1.6 の typing fix が前倒される
- 2026-04-26 v3 で Wave 3 forward に再分類 (Director Edit 2、user 明示 approve)

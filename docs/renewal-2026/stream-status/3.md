# Stream 3 Status — Visual Language Design System

| 項目 | 値 |
|---|---|
| 親計画 | `life/.claude/plans/portfolio-renewal-2026-04.md` §7.3 |
| 最終更新 | 2026-04-26 (Wave 2 完了時) |
| 完了状態 | 🟡 D3.2/D3.3/D3.4/D3.6 landed / D3.1 [~] partial (corner treatment Wave 3 forward) / D3.5 v3 で完全 de-scope |
| Deliverables | **5** (v3 で 6→5、D3.5 Storybook を user 明示 approve 経由 de-scope) |

## Deliverables

- [~] **D3.1** Tailwind 4 plugin で dot-new palette tokens を expose → **partial** (Corner treatment 残、2026-04-26 user 明示 directive 経由 re-classify)
  - **Wave 1 で landed** (palette / typography / motion grammar / spacing / z-index tokens):
    - `packages/design-system/src/tokens/index.ts` 拡充 (PALETTE / HEX_GRID / MOTION_GRAMMAR + THEME / SPACING / RADIUS / Z_INDEX)
    - `packages/design-system/src/tailwind/{plugin,index}.ts` 新規 (Tailwind 4 plugin、`:root` light vars + `[data-theme="dark"]` ゲート + utility classes)
    - `apps/web/tailwind.config.ts` 新規、design-system plugin 配線
    - typecheck: `bunx tsc --noEmit -p packages/design-system/tsconfig.json` → 0 errors
  - **Wave 2 finalize 残** (Corner treatment philosophy、plan §5.6.1):
    - `RADIUS` token を rebuild: `pill (999px)` と `panel (1.6rem)` を撤去、`SHARP (0)` / `HAIRLINE (1-2px)` / `superellipse (n=2.5、SVG path)` / `squircle (continuous、optional)` で代替
    - `apps/web/src/app/globals.css` の `border-radius 999px / 9999px / 50% / 1.2rem` 計 ~10 箇所 audit + 撤去 or sharp 化
    - `apps/web/src/app/[locale]/works/installation/page.tsx` の `rounded-lg` を撤去 (Wave 1 で見落とし、disk-verify audit で発見)
    - `<Squircle>` utility component を design-system に追加 (用途限定: touch target / logo 継承)
    - 新 IA route 6 file + 6 motion-studies で `rounded-*` class 使用 0 件を audit
  - 参照: plan §5.6.1 Corner treatment philosophy、`docs/logomark/logomark-curve-refinement-handoff.md` §5.2 / §8.1-2 / §9.1-2、`life/docs/guides/2026-04-17-motion-grid-cluster-ui-design-failure-handoff-full.md` §6.2.1 / §9.2 NO LIST
  - **Director correction (2026-04-26 user 明示 directive 経由)**: 旧 [x] は palette/typography 軸では完了相当だが、corner treatment 軸が未着手 → 全体としては [~] partial に下方修正。Anti-Drift Discipline §0.3 上は user 明示 directive 経由なので合法、§7.0 上は corner 軸の deliverable 化が明示されていなかった分の補完

- [x] **D3.2** Typography scale 移管 → **Wave 2 で finalize landed** (Agent γ)
  - Wave 1 landed: `packages/design-system/src/tokens/typography.ts` 新規 (TYPOGRAPHY / FONT_FAMILY / FONT_WEIGHT / LETTER_SPACING / LINE_HEIGHT)
  - Wave 2 landed: `packages/design-system/src/tailwind/plugin.ts` に `typographyVars()` 関数追加、Tailwind 4 plugin の `addBase` で `:root` に CSS variable 注入、`apps/web/src/app/globals.css` line 40-48 の clamp 7 件の定義を撤去 + migration TODO comment 撤去
  - **採用 pattern**: Pattern A (design-system plugin が CSS variable expose、globals.css は var() 参照のみ)
  - **visual regression リスク**: ゼロ (TYPOGRAPHY token の clamp 値は globals.css と完全 bit-identical)

- [x] **D3.3** Light mode default 化、dark mode alt 提供 → **Wave 2 で finalize landed** (Agent α、Option A 採用)
  - Wave 1 landed: `[locale]/layout.tsx` の `data-theme="light"` 既定 + `globals.css` の Filmtone dark glass tokens を `[data-theme="dark"]` selector 配下に gate + `:root` Light mode primary tokens + `prefers-color-scheme: dark` で auto-dark
  - **Wave 2 finalize**: `apps/web/src/app/[locale]/works/filmtone/layout.tsx` に `<div data-theme="dark" className="film-lab-lp-root">` wrapper 付与 (Option A、user 明示 approve 経由)
  - 効果: Filmtone segment 内で `[data-theme="dark"]` selector が活性、dark glass tokens 復元、Filmtone visual regression 解消
  - **採用しなかった Option B** (token migration via design-system dark variant): Filmtone 独立ドメイン化時に再検討

- [x] **D3.4** Component primitives 4 種実装 (Storybook 削除に伴い Definition 簡素化)
  - landed: `packages/design-system/src/components/{Logo,Wordmark,SoundToggle,NavRail}.tsx` 全 4 件
  - 全て currentColor 対応、props 設計済
  - SoundToggle は state prop で `silent | playing | muted` 切替可 (Wave 2 D5.4 で actual audio logic 接続)
  - Storybook での render 確認は v3 de-scope に伴い不要、`/craft` ページが catalog 機能を兼ねる予定

- ~~D3.5~~ **削除済 (v3, 2026-04-26)** — Storybook config + 4 stories + .storybook/ + 関連 devDeps
  - 削除理由 (user 明示 approve、Director Edit 1):
    - primitive 数 4 / 外部消費なし
    - `/craft` ページが catalog 機能を兼ねる
    - portfolio identity (作品体験 / craft signature) と逆方向の工業ツール体裁
  - 削除実施: Wave 1 完了時に `.storybook/`、`*.stories.tsx` 4 file、storybook scripts (storybook / build-storybook)、storybook devDeps (@storybook/addon-essentials / @storybook/react-vite / storybook)、`tsconfig.json` の `*.stories.tsx` exclude を撤去
  - 母数影響: 全体 D{N}.{n} 36 → 35、Stream 3 deliverables 6 → 5

- [x] **D3.6** `apps/web/src/components/Logo.tsx` を design-system Logo に置換
  - landed: working tree
  - re-export 形式: `export { Logo as default, Logo, type LogoProps, type LogoVariant } from "@chibatakumi/design-system"`

## Definition of Done (v3 + 2026-04-26 corner directive)

D3.2 / D3.3 / D3.4 / D3.6 全 closed (Wave 1 + Wave 2、達成済) + D3.1 Corner treatment finalize (**Wave 3 forward**、本 Wave 2 では着手せず、user 明示 directive 経由で scope 追加されたが Wave 2 plan 後の追加のため Wave 3 で実施)。

## Notes

- Stream 3: Wave 2 終了時 4/5 closed (D3.1 corner treatment Wave 3 残)
- Storybook 9→8 downgrade は registry constraint で v3 de-scope により無効化
- Filmtone dark glass tokens 衝突は Wave 1 `[data-theme="dark"]` ゲート + Wave 2 wrapper 付与で正式解決
- D3.1 corner treatment は元々 [x] (palette/typography 軸完了) だったが、2026-04-26 user 明示 directive で corner 軸が追加され [~] downgrade、Wave 2 plan には含まれなかったため Wave 3 forward で実施

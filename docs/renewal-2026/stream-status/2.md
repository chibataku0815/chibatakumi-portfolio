# Stream 2 Status — motion-dot-new-webgpu Package 化

| 項目 | 値 |
|---|---|
| 親計画 | `life/.claude/plans/portfolio-renewal-2026-04.md` §7.2 |
| 最終更新 | 2026-04-26 (Wave 2 完了時) |
| 完了状態 | 🟡 D2.1-D2.6 landed (6/8) / D2.7 [~] Wave 2 smoke 通過 (formal Wave 3) / **D2.8 [~] partial** — Phase A+2 wiring + 3 regression fixes landed だが quality parity 未達 (user feedback 2026-04-26 経由)、別 chat (`motion-dot-quality-remediation-handoff.md`) で remediation 継続 |
| Deliverables | **8** (v4 で D2.8 Phase A+2 追加、母数 35→36) |

## Deliverables

- [x] **D2.1** `life/output/motion-dot-new-webgpu` source を `packages/motion-dot/src/` に vendor
  - landed: working tree、9 subdir (animation, audio, compute, input, render, scene, shaders, transition, ui) + wgsl.d.ts
  - 未 commit (Stream 4 D4.6 commit `281a4d0a` で同梱の可能性、要 verify)

- [x] **D2.2** package.json deps 6 件配線
  - landed: `@chibatakumi/motion-core` + `gpu-2.5d-presets` + `gpu-fx-presets` + `webgpu-motion-{audio,dom,input}`

- [x] **D2.3** `createDotScene(name, device, opts)` REAL factory
  - landed: `packages/motion-dot/src/index.ts`、17 scene name-dispatch
  - 含む: 16 vendor preset (gpu-fx-presets) + 1 local (`fluid` from createFluidScene)

- [x] **D2.4** `DotScene` 統一 adapter contract
  - landed: vendor preset (MetaballParticleSource) + local FluidScene を single shape に collapse

- [x] **D2.5** `createDotParticipant` scaffold
  - landed: motion-grid / motion-flow と parity な MotionParticipant<DotParam> shape
  - Phase A wiring（init body 実装）は Stream 4 D4.6 で landed 済み

- [x] **D2.6** kinetic-handoff + composite-25d + fluid-scene を re-export
  - landed: src/index.ts から createKineticHandoff / createGalleryMode / createFluidScene + types

- [~] **D2.7** portfolio shell 経由の demo route で 1 scene 動作 → **Wave 2 smoke 通過 / Wave 3 formal 残**
  - **Wave 2 smoke (Wave 2 main chat)**: `bun run --cwd apps/web dev` → `curl -o /dev/null -w "%{http_code}" http://localhost:3000/experiments/dot` → **200 OK** 確認
  - **Wave 3 formal 残**: kinetic-handoff scene cycle / composite-25d gallery mode / 17 scene 全切替 / film post 配線 を browser で確認、e2e selector 整備

- [~] **D2.8** Phase A+2 wiring + Quality regression fix (**partial** — Wave 1 後段、user 明示 approve 経由で plan v4 §7 realign。**2026-04-26 user feedback「まだクオリティが追いついてない」経由で別 chat へ remediation 引き継ぎ**、参照: `docs/renewal-2026/motion-dot-quality-remediation-handoff.md`)
  - **Phase A+2 wiring landed** (agent + 直接実装):
    - `packages/motion-dot/src/index.ts` に `enableGalleryMode` + `panelCount` + `galleryScenes` options 追加、`createGalleryMode` (composite-25d) を init/update/render/dispose で wire (+378 lines initial)
    - keyboard cluster: `enableInput` を `void` から実装に昇格、document keydown listener (ArrowRight/Space/n: next、ArrowLeft/p: prev、r: reset)、isInputElement gate 付き
    - `/experiments/dot/client.tsx` を gallery mode (panelCount=4) に切替 (showcase peak)、`enableInput: true` 維持、`enableHud: true` は撤去 (HUD は Wave 3 forward)
  - **Quality regression fix landed (2026-04-26 user feedback「美しいシーンとシーンの繋がりが再現できていない / シーン自体のクオリティが落ちている」経由で 3 critical 同定 + 修正)**:
    1. **AudioBus demoStyle "beat" 復元**: original main.ts:302 で明示されていた `new AudioBus({ demoStyle: "beat" })` ("dot 120 BPM silent-time aesthetic") が現 MotionStage default "ambient" に flip していた regression。`apps/web/src/features/motion/MotionStageProvider.tsx` で `createMotionStage({ ..., demoStyle: "beat" })` を明示的に渡して復元
    2. **TRANSITION_ANCHOR_POLICIES scene name mismatch 修正**: policy keys は Title Case ("River Flow" / "Pendulum Wave" / "River Delta" / "Chain") だが、現 motion-dot は lowercase DOT_SCENE_NAMES を transitionScenes name に渡しており **全 transition が DEFAULT policy fall-through、per-scene anchor / orbit / wobble / burst tuning が完全消失**。`DOT_SCENE_DISPLAY_NAMES` map (17 entries: orbit→Orbit / river→River Flow / pendulum→Pendulum Wave / chain→Chain / delta→River Delta / helix→DNA Helix / firefly→Firefly Sync / text-attractor→Living Typography / grid-fluid→Grid Fluid 等) を追加、cycleSceneNames を display name 経由に変更
    3. **INTENSITY_TUNING gallery damping coefficients 全 8 fields 移植**: original main.ts:87-99 の `galleryParticleLift / galleryThresholdDamping / gallerySoftnessDamping / galleryRimDamping / galleryBloomDamping / galleryGrainDamping / galleryChromaDamping / galleryVignetteDamping` を port、`createPresentationModulation(panelCount, shapedIntensity)` 関数で galleryMix-based damping 計算、`GALLERY_SCENE_DAMPING` per-scene-index map (idx 1, 6 の particleIntensity / threshold / softness / rim 個別 override) + `applyGallerySceneDamping` を移植、render() 全面書換: hardcoded modulation を createPresentationModulation 経由に統一、gallery panel renderer で per-panel `applyGallerySceneDamping(sceneIdx, modulation)` を実行
  - audio reactive 検証: MotionStage の audioBus + audioState は live 配線済 (motion-core/src/stage/index.ts L244 audioBus.update + L249 audioState.intensity = audioBus.intensity)、createDotParticipant が DOT_WIRING.resolveInto + setAudioReactive で消費中 → **demoStyle: "beat" 復元で正しい envelope に戻った**
  - typecheck: motion-dot 0 errors / apps/web baseline 4 errors 維持 / build verify 全 route prerender 成功
  - HUD overlay は **外殻**として Wave 3 forward (user 指示「外殻最小限」)
  - 母数: Stream 2 deliverables 7→8、全体 35→36

## Definition of Done (v4)

D2.1 〜 D2.6 + D2.8 全 closed (達成済 / Phase A+2 agent 完了確認後確定) + D2.7 Wave 3 formal で closed + motion-dot `bunx tsc --noEmit` 0 errors (達成済)。

## Notes

- typecheck 0 errors 確認済 (Wave 1 audit)
- 未 commit のため D2.1〜D2.6 の commit ref は記録できない (Wave 1 の logical commit に同梱予定)
- Canvas2D 版（life/output/motion-dot-new）は削除せず、Wave 2 で `/journal/motion-studies/dot-new-canvas2d` archive として配置検討
- 2026-04-26 v3 で D2.7 を Wave 2 smoke + Wave 3 formal に二段化 (Director Edit 2、user 明示 approve)

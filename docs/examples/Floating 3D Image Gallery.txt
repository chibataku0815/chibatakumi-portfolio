

If you have been keeping up with awards, you might have come across this website that won a website of the day just last Thursday on January 30th.


From the moment you land on the site, you're greeted with some really impressive animations.


The one that stood out to me the most is this particular section where the content pins.


As soon as it enters the viewport, and as you scroll further, the titles start moving horizontally while the images animate into view with a 3D effect.


But that's not all. If you look closely, there is also a dynamic text highlight animation.


So as the titles move, a colorful trail appears based on the speed of the animation, adding another layer of depth to the experience.


In today's video, I'm going to show you how to recreate this exact scroll animation using GSAP and ScrollTrigger.


We'll be covering everything from the horizontal scroll animation on the titles to the colorful trail effect that follows the text based on speed, as well as the 3D animation applied to the image cards in the background.


At this point, you already know that I'm always on top of the latest trends, breaking down and decoding the newest animation from recent websites.


So if you find my work helpful, I would really appreciate it if you drop a like on the video and maybe even subscribe. It really helps the channel grow and reach more people.


And if you would like to access the source code, you can check out the pro membership via the link in the description.


All right, let's dive into the code.


So let's start by setting up the basic structure of the page. We'll need three sections, a hero section, a sticky section, and an outro section.


The hero section and the outro will have an H1 element with some placeholder text. These are just there to make sure the page doesn't look empty.


You can leave them blank if you prefer, but I'm adding some text just for better visual balance.


Next, inside the sticky section, we need to create two main containers. One will contain the titles and the other will be a container for all the image cards.


For the titles, we'll add a wrapper div with the class name title, and we need four title containers. Each of these will have three H1 elements with same text.


The reason we are using three layers of text is to create the colored trail effect.


The first and second H1 elements will have the animated colors applied to them, while the third one will stay on top in solid black, just like the original design.


I'll follow the same structure for all the title containers. So I'll just copy and adjust the text to make them look better.


For now, we'll leave the image container empty because we'll be generating the cards dynamically using JavaScript later. And that's it for the HTML setup.


Now let's move on to the styling.


First, we'll reset the default margins and paddings by setting everything to zero and applying box-sizing border-box to make sure the layout behaves consistently.


For the body, I'll set the custom font to match the original design.


Next, we'll ensure that all images take up their full container by setting their width and height to 100% and applying object-fit cover so they scale properly without distortion.


For the sections, each one will be set to take up the full width and height of the viewport with overflow hidden to ensure that elements don't extend beyond their containers.


The hero and outro sections will be centered using flexbox with a light background color to differentiate them from the rest of the layout.


The sticky section where the main animation happens will have a slightly off-white background to create a subtle distinction.


Next, we need to style the title container. The title container will be positioned absolutely at the top left corner and set to four times the viewport width, which allows the horizontal scrolling effect to happen.


We'll also use will-change transform to optimize performance when we animate it.


Each title container will be fully flexible and centered using flexbox.


The H1 elements inside will be also positioned absolutely, centered within their respective containers using transforms.


They will be styled with a large italic font and we'll apply will-change transform again to optimize scrolling performance.


To create the color trail effect, we'll assign different colors to each of the three H1 layers inside every title container. The first title layer will have a light neon green color, the second one will be a bright blue, and the topmost title which stays in place will be black.


Next, the image container will be positioned absolutely in the center of the viewport and set to two and a half times the viewport width and height to allow for the 3D effect.


We'll enable transform style preserve-3d and set a perspective value to give the images proper depth. The z-index will be set to negative one to ensure the images remain behind the text.


Finally, we'll style the individual image cards. Each card will have a fixed size and rounded corners. We'll also enable transform style preserve-3d so that we can animate them in 3D dimensions later.


To prevent any unwanted image overflow, we'll apply overflow hidden. And that's it for the CSS.


Now let's move on to the JavaScript to bring everything to life.


First, we need to ensure that our script runs only after the page has fully loaded, so we wrap everything inside DOMContentLoaded.


Now, let's start by setting up Lenis, which is a smooth scrolling library. This will help us achieve a more fluid scroll effect rather than the default browser scrolling behavior.


This block of code is directly taken from the Lenis documentation. So if you want to explore more about its settings and customization options, you can check out their official docs.


Now that we have smooth scrolling setup, let's move on to dynamically positioning the image cards that will animate into view as we scroll.


First, we define an array called cardPositions which holds the predefined positions for each card. These positions are specified using top and left percentages, ensuring that each card is placed at a unique location within the viewport.


This will give us a more scattered and organic layout rather than a rigid grid. Next, we store references to some key elements in the document. We select the titles container since we'll be animating it later, and we'll also define a variable called moveDistance which is set to three times the window width.


This will determine how far the titles container will move horizontally during the scrolling animation. Now we grab the images container which will hold all the dynamically generated image cards.


Instead of manually adding these cards in HTML, we create them in JavaScript using a loop. We loop from one to ten, and for each iteration we create a div element with a class of card and a unique class name based on its index.


Inside this div, we create an image element, setting its source to an image file with a corresponding number. These images are coming from the assets folder, following the naming format, image1.jpeg, image2.jpeg and so on.


Once the image is created, we append it inside the card, then retrieve its corresponding position from the cardPositions array and apply the top and left values accordingly. Finally, we append each card into the images container.


At this point, all ten cards are now inside the container positioned exactly as we defined earlier. But before we animate them, we need to set their initial state.


So we select all the cards using querySelectorAll and loop through them. Each card is set to start far away from the viewport with a Z value of negative 50000, making them invisible. We also set their scale to zero so they remain completely hidden until we animate them later.


Now that the cards are positioned and hidden, let's move on to handling the scroll animations.


We start by creating a new ScrollTrigger instance and set the trigger to the sticky section, since that's where our main animation happens.


The animation starts when the sticky section reaches the top of the viewport and it will last for a scroll distance equal to five times the height of the window to ensure the section stays fixed while scrolling. We enable pinning, and we also set scrub to one, which means the animation will smoothly follow the user scroll position instead of jumping between keyframes.


Now inside the onUpdate callback function, which runs on every scroll update, we start by calculating the horizontal movement of the titles. We take the scroll triggers progress, which is a value between zero and one, and multiply it by negative moveDistance, which we defined earlier as three times window width.


This ensures that as we scroll, the title smoothly moves from right to left across the screen.


Next, we introduce velocity-based animations to enhance the scrolling effect. We first retrieve the current velocity using getVelocity function, which tells us how fast the user is scrolling.


To avoid issues when the velocity is zero, we normalize it by dividing it by its absolute value, ensuring it always results in either minus one, zero, or one. We define a max offset value of 30 pixels to prevent the effect from becoming too extreme, and we calculate the current speed by taking the absolute value of velocity, dividing it by 500 and capping it at the max offset.


This means the faster the user scrolls, the larger the effect, but only up to a certain limit. To ensure smooth transitions when scrolling starts or stops, we check whether the progress is at either edge, meaning zero or one.


If it is, we reset the animation for the first two title layers, bringing them back to their original position.


If the user is actively scrolling, we take the normalized velocity and multiply it by current speed to create a base offset. This offset is then applied to the first two layers of the title, creating the dynamic trailing effect. The first layer moves four times the offset, while the second one moves twice the offset, making the color trail effect feel more natural.


The third title layer, which remains black, is always kept at zero to maintain readability. Now that we have the horizontal scroll and text trail effect working, let's move on to animating the image cards.


We start by looping through each card using forEach method and apply staggered animations to make them appear at different points during the scroll. First, we define a stagger offset, which is calculated by multiplying the cards index by 0.075.


This ensures that each card enters the animation slightly later than the one before it, creating a smooth cascading effect rather than all images appearing at once. Next, we calculate the individual progress of each card.


Since ScrollTrigger gives us a progress value between zero and one, we adjust this for each card by subtracting the trigger offset and multiplying it by three. This helps spread out the animations more naturally.


We then clamp the result between zero and one to prevent any unexpected values. Now, we determine the Z position for each card. The final card in the array will be placed slightly closer to the viewer at 1500 pixels, while the others will go to 2000 pixels in 3D space.


We calculate a new Z position by starting at minus 50000 pixels and gradually moving them forward as they scroll into view. This makes the images feel like they're emerging from very far, adding a parallax-like depth effect. We also apply scaling effect to make the images appear as they move closer. We take the individual progress, multiplying it by ten and clamping it between zero and one to avoid oversized scaling.


This ensures that each card gradually scales up from zero to full size as it comes into view. Finally, we use the GSAP set method to apply the new Z position and scale values to each card in real time as the user scrolls.


This creates a smooth 3D depth effect, making the images feel like they're dynamically floating into the scene.


And with that, the entire scroll animation is complete. Hope you found the video helpful. See you in the next one.

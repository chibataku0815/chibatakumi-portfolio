# Task 1.1: 404 Page 実装

**フェーズ:** Phase 1 - Quick Wins
**優先度:** ★★★★★ (Critical)
**期間:** 2-3日
**前提条件:** なし
**Level 貢献:** L3.5 → L4（Craft Details の証明）

---

## 🎯 目的

Pitch Black & Fire の世界観を保った、Award-Worthy レベルの 404 ページを実装する。

**Excellence Framework 基準:**
> 404ページまで設計されているか？
> これがないと Level 4 に到達できない。

**参照サイト:**
- Dennis Snellenberg: 404ページまで完璧なクラフト
- Aristide Benoist: エラー状態も体験の一部

---

## 📋 要件定義

### 機能要件
- [ ] `/apps/web/src/app/not-found.tsx` を作成
- [ ] 404 エラー発生時に自動表示
- [ ] ホームへの誘導 CTA
- [ ] PageTransition 適用

### デザイン要件
- [ ] Pitch Black & Fire の世界観維持
- [ ] 静謐だが希望を感じさせる表現
- [ ] カーソル追従の光源エフェクト
- [ ] Hero shader の静的版を背景に使用

### 技術要件
- [ ] Next.js App Router の `not-found.tsx` 規約準拠
- [ ] Hero shader コンポーネントの再利用
- [ ] GSAP でテキストアニメーション
- [ ] レスポンシブ対応

---

## 🎨 デザインコンセプト

### Visual Metaphor
```
"迷い込んだ暗闇の中で、微かな光が道を示す"

- 背景: Hero shader の最も静謐な状態
- 中央: "404" または "Lost in the Abyss" のタイポグラフィ
- カーソル追従: 小さな光源がユーザーの動きを照らす
- CTA: ホームへの「道標」として Amber で光る
```

### Mood Dimensions
```
Temperature:  ■■□□□□□□□□  (Cold, but not hostile)
Density:      ■■■■■□□□□□  (Dense, contemplative)
Rhythm:       ■■□□□□□□□□  (Slow, breathing)
Contrast:     ■■■■■■■□□□  (High, focused attention)
Intimacy:     ■■■■■■□□□□  (Close, personal)
```

---

## 🏗️ 実装仕様

### ファイル構成
```
apps/web/src/app/
└── not-found.tsx              # メインファイル（新規作成）

apps/web/src/features/hero/
└── components/
    ├── HeroShaderBackground.tsx  # 既存（再利用）
    └── StaticHeroShader.tsx      # 新規（静的版、optional）
```

### コンポーネント構造
```tsx
export default function NotFound() {
  return (
    <main className="relative min-h-screen flex items-center justify-center">
      {/* Background: Hero shader の静的版 */}
      <HeroShaderBackground /> {/* または StaticHeroShader */}

      {/* Cursor-following light source */}
      <CursorLight />

      {/* Content */}
      <div className="relative z-10 text-center">
        <AnimatedHeading as="h1">
          404
        </AnimatedHeading>

        <p className="text-[var(--text-base-60)]">
          Lost in the Abyss
        </p>

        <Link
          href="/"
          data-transition="true"
          className="amber-border-glow"
        >
          Find Your Way Home
        </Link>
      </div>
    </main>
  );
}
```

### カーソル追従光源の実装
```tsx
// components/CursorLight.tsx (新規)
"use client";

import { useEffect, useRef } from "react";
import gsap from "gsap";

export function CursorLight() {
  const lightRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const handlePointerMove = (e: PointerEvent) => {
      gsap.to(lightRef.current, {
        x: e.clientX,
        y: e.clientY,
        duration: 0.6,
        ease: "power2.out",
      });
    };

    window.addEventListener("pointermove", handlePointerMove);
    return () => window.removeEventListener("pointermove", handlePointerMove);
  }, []);

  return (
    <div
      ref={lightRef}
      className="pointer-events-none fixed -translate-x-1/2 -translate-y-1/2 z-0"
      style={{
        width: "600px",
        height: "600px",
        background: "radial-gradient(circle, rgba(255,191,73,0.15) 0%, transparent 70%)",
        filter: "blur(80px)",
      }}
    />
  );
}
```

---

## 📐 実装手順

### Step 1: ファイル作成（30分）
```bash
# ファイル作成
touch apps/web/src/app/not-found.tsx

# カーソルライトコンポーネント作成（optional）
mkdir -p apps/web/src/features/error-pages/components
touch apps/web/src/features/error-pages/components/CursorLight.tsx
```

### Step 2: 基本構造実装（1時間）
```tsx
// apps/web/src/app/not-found.tsx
import { HeroShaderBackground } from "@/features/hero/components";
import { AnimatedHeading } from "@/shared/components";
import Link from "next/link";

export default function NotFound() {
  return (
    <main className="relative min-h-screen flex flex-col items-center justify-center px-6">
      {/* Background */}
      <HeroShaderBackground />

      {/* Content */}
      <div className="relative z-10 flex flex-col items-center gap-8 text-center">
        <AnimatedHeading
          as="h1"
          className="text-[clamp(8rem,20vw,16rem)] font-bold leading-none tracking-tighter text-[var(--text-base-20)]"
        >
          404
        </AnimatedHeading>

        <div className="flex flex-col gap-4">
          <p className="text-2xl font-medium text-[var(--text-base-70)]">
            Lost in the Abyss
          </p>
          <p className="text-base text-[var(--text-base-50)] max-w-md">
            The page you're looking for has dissolved into the darkness.
          </p>
        </div>

        <Link
          href="/"
          data-transition="true"
          className="amber-border-glow mt-4 inline-flex items-center gap-2 rounded-full border border-[var(--text-base-20)] px-8 py-4 text-sm font-medium uppercase tracking-[0.12em] text-[var(--text-base)] transition-all duration-300 hover:border-[var(--accent-amber1)]/60 hover:text-[var(--accent-amber1)]"
        >
          <svg className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 19l-7-7m0 0l7-7m-7 7h18" />
          </svg>
          Find Your Way Home
        </Link>
      </div>
    </main>
  );
}
```

### Step 3: カーソルライト実装（1-2時間、optional）
```tsx
// apps/web/src/features/error-pages/components/CursorLight.tsx
"use client";

import { useEffect, useRef } from "react";
import gsap from "gsap";

export function CursorLight() {
  const lightRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const handlePointerMove = (e: PointerEvent) => {
      if (!lightRef.current) return;

      gsap.to(lightRef.current, {
        x: e.clientX,
        y: e.clientY,
        duration: 0.6,
        ease: "power2.out",
      });
    };

    window.addEventListener("pointermove", handlePointerMove, { passive: true });
    return () => window.removeEventListener("pointermove", handlePointerMove);
  }, []);

  return (
    <div
      ref={lightRef}
      className="pointer-events-none fixed -translate-x-1/2 -translate-y-1/2 z-0"
      style={{
        width: "600px",
        height: "600px",
        background: "radial-gradient(circle, rgba(255,191,73,0.15) 0%, transparent 70%)",
        filter: "blur(80px)",
      }}
      aria-hidden="true"
    />
  );
}

// not-found.tsx に追加
import { CursorLight } from "@/features/error-pages/components/CursorLight";

// <main> 内の <HeroShaderBackground /> の下に追加:
<CursorLight />
```

### Step 4: テスト（30分）
```bash
# ローカルで確認
bun dev

# 存在しないパスにアクセス
# 例: http://localhost:3000/this-does-not-exist

# 確認項目:
# - 404 ページが表示される
# - PageTransition が機能する（"Find Your Way Home" クリック時）
# - カーソルライトが追従する（optional）
# - レスポンシブ対応
```

---

## ✅ 完了基準

### 必須項目
- [ ] `not-found.tsx` が実装されている
- [ ] 存在しないパスで 404 ページが表示される
- [ ] Pitch Black & Fire の世界観が維持されている
- [ ] ホームへのリンクに PageTransition が適用されている
- [ ] レスポンシブ対応（モバイル〜デスクトップ）

### 推奨項目
- [ ] カーソル追従の光源エフェクト
- [ ] テキストアニメーション（GSAP）
- [ ] Hero shader の静的版使用

### Quality Check
- [ ] Typography が読みやすい
- [ ] CTA ボタンが明確
- [ ] ユーザーが迷わない導線
- [ ] パフォーマンス影響なし（LCP < 2.5s）

---

## 🎨 デザインバリエーション（Option）

### Option A: Minimal（推奨）
```
- 大きな "404" タイポグラフィ（Ghost opacity）
- 簡潔なメッセージ
- Amber の CTA
```

### Option B: Poetic
```
- "Lost in the Abyss" を大きく
- 404 は小さく副次的に
- より詩的なメッセージ
```

### Option C: Interactive
```
- カーソルライトがより強調
- 404 の文字がカーソル追従で浮き上がる
- より体験的
```

**推奨: Option A（シンプル、実装コスト低）**

---

## 📚 参照リソース

### Next.js ドキュメント
- [not-found.js](https://nextjs.org/docs/app/api-reference/file-conventions/not-found)

### 参照サイト
- **Dennis Snellenberg 404**: 完璧なクラフトの例
- **Aristide Benoist 404**: ページ遷移が美しい

### プロジェクト内参照
- `apps/web/src/features/hero/components/HeroShaderBackground.tsx`
- `apps/web/src/shared/components/AnimatedHeading.tsx`
- `apps/web/src/app/globals.css` (amber-border-glow クラス)

---

## 🚨 注意事項

### パフォーマンス
- Hero shader が重い場合は静的版を作成
- カーソルライトは `passive: true` でイベント登録

### アクセシビリティ
- `<h1>` で 404 を明示
- CTA は `<Link>` で実装（キーボードナビゲーション対応）
- カーソルライトは `aria-hidden="true"`

### 世界観の一貫性
- 「エラー」ではなく「発見」のトーンで
- "Lost" は negative ではなく contemplative に
- Amber の光は「希望」「道標」のメタファー

---

## 📝 完了後のアクション

1. **スクリーンショットを撮る**: ドキュメント用
2. **README.md を更新**: Phase 1 進捗を記録
3. **次のタスクへ**: [02-loading-page.md](./02-loading-page.md)

---

**Status:** 🔜 Not Started
**Assigned:** -
**Started:** -
**Completed:** -
